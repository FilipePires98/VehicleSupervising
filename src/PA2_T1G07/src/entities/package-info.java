/**
 * Provides all supported entities, including those that collect data and produce information to others (CollectEntity) and those that consume such information (BatchEntity, ReportEntity and AlarmEntity).
 */
package entities;
